# nvim-config-lazyvim
My old nvim configuration built on LazyVim. Replaced by a minimal config using vim.pack

It is a slightly modified LazyVim config with few added plugins and keymaps.

Follows the same directory structure, all installed plugins can be found in lua/plugins

If you wish to use it yourself simply clone the files into your ~/.config folder.
